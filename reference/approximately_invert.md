# Matrix inversion with cascading fallbacks

Matrix inversion with cascading fallbacks

## Usage

``` r
approximately_invert(x)
```

## Arguments

- x:

  Square matrix

## Value

List with `inverse` and `replacement` (NULL if exact, string if fallback
used)
