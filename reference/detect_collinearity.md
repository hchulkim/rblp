# Detect collinearity via QR decomposition

Detect collinearity via QR decomposition

## Usage

``` r
detect_collinearity(x)
```

## Arguments

- x:

  Matrix

## Value

List with `detected` (logical) and `indices` (which columns to drop)
