# Extract numbered columns from data frame into matrix

Extract numbered columns from data frame into matrix

## Usage

``` r
extract_columns(data, prefix)
```

## Arguments

- data:

  Data frame

- prefix:

  Column name prefix (e.g., "demand_instruments")

## Value

Matrix of matched columns, or NULL if no match
