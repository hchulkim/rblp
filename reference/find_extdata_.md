# Find and load extdata file (works in both installed and development mode)

Find and load extdata file (works in both installed and development
mode)

## Usage

``` r
find_extdata_(filename)
```

## Arguments

- filename:

  CSV file name

## Value

Data frame
