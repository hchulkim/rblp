# Gauss-Hermite quadrature points and weights

Gauss-Hermite quadrature points and weights

## Usage

``` r
gauss_hermite(n)
```

## Arguments

- n:

  Number of points

## Value

List with nodes and weights for integrating f(x) \* exp(-x^2)
